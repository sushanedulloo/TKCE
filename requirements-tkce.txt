# TKCE experimental framework (tkce/, run_suite.py, run_slice.py)
# Install on the GPU server, e.g.:
#   conda create -n tkce python=3.11 -y && conda activate tkce
#   pip install -r requirements-tkce.txt
#   # torch: pick the CUDA build for your server from https://pytorch.org/get-started/locally/
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
torch>=2.0
xgboost>=2.0
catboost>=1.2
lightgbm>=4.0
openml>=0.14
optuna>=3.5
matplotlib>=3.7
tqdm>=4.66
